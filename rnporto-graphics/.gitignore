node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Saved gallery entries — keep the directory, ignore the contents.
data/gallery/*
!data/gallery/.gitkeep
